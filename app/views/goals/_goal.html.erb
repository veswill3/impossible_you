<li>
  <span class="content"><%= link_to goal.title, goal %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(goal.created_at) %> ago.
  </span>
  <% if current_user?(goal.user) %>
    <%= link_to "delete", goal_path(goal), 
    																 method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: goal.title %>
  <% end %>
</li>